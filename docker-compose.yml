version: '3'

services:
  web:
    # container_name: web
    # build: ./docker/Dockerfile-local
    build:
      context: ./docker
      dockerfile: Dockerfile-local
    ports:
      - 80:80
    privileged: true
    environment:
      APP_ENV: local
    volumes:
      - ./:/var/www/html
     


